import random
min = 1
max = 6

roll = "yes"

while roll == "yes" or roll == "y":
    print ("Rolling...")
    print ("Dice value is....")
    print (random.randint(min, max))

    roll_again = input("Roll the dice ?")
